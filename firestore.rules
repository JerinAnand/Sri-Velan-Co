rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Anyone (public site visitors) can READ all siteContent documents
    // Only authenticated admin users can WRITE to siteContent documents
    match /siteContent/{document=**} {
      allow read: if true;
      allow write: if request.auth != null;
    }
  }
}

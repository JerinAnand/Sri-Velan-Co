{
  "entities": {
    "SiteSection": {
      "title": "Site Section Content",
      "description": "Stores content for various sections of the web application",
      "type": "object",
      "properties": {
        "title": { "type": "string" },
        "subtitle": { "type": "string" },
        "description": { "type": "string" }
      }
    },
    "Translations": {
      "title": "Translations Data",
      "description": "Stores key-value translation dictionaries for English and Tamil",
      "type": "object",
      "properties": {
        "en": { "type": "object", "description": "English translation dictionary" },
        "ta": { "type": "object", "description": "Tamil translation dictionary" }
      }
    }
  },
  "firestore": {
    "/siteContent/{sectionKey}": {
      "schema": "SiteSection",
      "description": "Collection storing site content sections, including hero, about, services, stats, clients, governingBoard, projects, contact, footer, translations, equipment, capabilityStatement, hydraulicBroomer, weatherAlertBanner, chatbot, and navigation."
    },
    "/siteContentHistory/{historyId}": {
      "schema": "SiteSection",
      "description": "Collection storing historical change logs and backup snapshots of site content."
    },
    "/systemConfig/{configDoc}": {
      "schema": "SiteSection",
      "description": "System configuration documents, including defaultContent snapshot."
    }
  }
}
